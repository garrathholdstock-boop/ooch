node_modules/
*.log
.DS_Store
.env
caddy_data/
caddy_config/

# Uploads and the photo/state maps live in /srv/ooch/data, outside the repo,
# so a clone never carries someone's camera roll and a deploy never overwrites it.
data/
uploads/
